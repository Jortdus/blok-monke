<!DOCTYPE html>
<html lang="en">
<head>
    
</head>
<body>

    <header>
        <%- include('../partials/header');%>
    </header>

    <main>   
        <section>
            <h2>Welcome </h2>
            <h1>People you Liked</h1>
            <h3>Filter</h3>
        </section> 

        <section>
            <ul>
                <% personen.forEach(function(persoon) { %>
                <li>
                    <img src="<%= persoon.foto %>" class="profielfoto" alt="Profielfoto van fabian vis"/>
                    <h1><%= persoon.gamertag%></h1>
                    <h1><%= persoon.genre%></h1>
                    <button data-id="<%=persoon._id%>">Remove</button>
                </li>
                <% });%>
            </ul>
        </section>  
    </main>
    <footer>
        <%- include('../partials/footer');%>
    </footer>
    <script src="../../static/script/deleteButton.js"></script>
</body>
</html>